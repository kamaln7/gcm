package gcm.client;

public class Settings {
    public String host;
    public Integer port;

    public Settings(String host, Integer port) {
        this.host = host;
        this.port = port;
    }
}
